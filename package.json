{
  "name": "epns-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://ipfs.infura.io:5001 vbstreetz/epns",
    "deploy-staging": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://ipfs.infura.io:5001 vbstreetz/epns-staging"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
